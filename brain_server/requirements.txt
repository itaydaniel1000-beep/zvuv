# Full-brain server (brain_server/server.py). Tested with Python 3.11 / 3.12.
brian2==2.9.0
numpy>=1.24,<2.4   # brian2 2.9.0 breaks with numpy 2.4 (ndarray.ptp was removed)
pandas>=2.0
pyarrow>=14
scipy>=1.10
websockets>=12
cython>=3.0        # only used if a C++ compiler is installed (much faster)
